Empty by design. This entry is analysis-first: there is no raw per-respondent model export.
The Tier-1 rows in predictions/ are synthesised backwards from the predicted effect table
(run_record/target_model.py -> run_record/target_entry.py --exact), so the prediction file *is* the raw output.
