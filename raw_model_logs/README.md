# Raw model logs (registration item K.2) — ESCROWED (class B)

This entry was produced by an autonomous agent over seven sessions (2026-08-27/28) plus one
internal-test fork and one head-to-head fork. The complete session transcripts (the harness's JSON event logs of every message, tool call and
sub-agent update, plus the Prime Agent's own session JSONLs with model/thinking records and
per-call sub-agent usage), the oracle and gate records, both forks' task directories and the
launch snapshots — 161 files, 305 MB — are deposited as a separate **restricted-access Zenodo record** (DOI in
`registration.md` K.2), because they quote verbatim stimulus texts of validation studies released
without a licence. The public commitment to their content is the SHA-256 of the bundle's
`MANIFEST.sha256`, recorded in `registration.md` K.2; the manifest itself is reproduced here:
`MANIFEST.sha256` (this directory). Access is available to the benchmark organizers on request.

Note that the entry itself is a deterministic function of `run_record/anchors/*.csv` and
`run_record/target_model.py` + `run_record/target_entry.py`; the transcripts document how those were
written, not any per-respondent generation (none exists).
