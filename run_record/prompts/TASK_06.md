# TASK_06 — resolve A18, test A16, and say whether you would stand behind it

Session s5's REPORT.md was read in full. Operator answers first.

## Operator answers (REPORT §5)

1. **The gate has run on your declared pair.** Verdict and category are in
   `runs/20260828T060726Z_s5/gate_m10-targeting.json`. Your pre-registered reading of a
   REJECTED was noted before the verdict existed; it is yours to apply.
2. **A18 must be resolved this session, explicitly.** Entry v3's `A_MULT`/`S_MULT` derive
   from M10, and the gate did not promote M10. The frozen definitions say techniques
   persist through the gate. Reconcile this in DESIGN.md in one of two ways, in writing:
   revert the entry's direction change to the promoted state; or defend, from the frozen
   text itself, why a pre-registered matched-amplitude measurement governs an *entry
   constant* even where the gate declined the *mechanism*. Either answer is acceptable;
   an unexamined middle is not.
3. **Budget approved:** your first-choice child — **A16**, testing whether any half-tier
   content-targeting map for the 16 target texts can be *validated* (LOSO discipline, as
   ANCHORS_H/J) rather than authored. If it fits alongside, the recipient-alignment read
   (A14's residual, ±0.7 pp on 16 cells) as second. Same scoping rules. Nothing for
   `claude -p` without asking.

## This session

1. **A18 first** (answer 2). Whatever you decide governs what v4 looks like.
2. **The A16 child**, and fold its answer into the entry honestly — including the answer
   "no map validates", which per your own §6 would leave v3's four-cell restraint as the
   right posture.
3. **Entry v4** through the full pipeline if anything material changed; otherwise state
   that v3 stands and why.
4. **Scored calls:** yours to spend as questions under your R-rules, or to decline with
   reasons. Nothing this session requires them.
5. **Report** as always — and end §"what separates this arm" with a plain declaration:
   is this the method and entry you would stand behind as-is, and if not, name the single
   thing that would change your answer. The operator reads that sentence first.

Budget for this session: the launcher's defaults.
