# TASK_04 — after the first promotion

Session s3's REPORT.md was read in full. Operator answers first.

## Operator answers (REPORT §6)

1. **The gate has run on your declared pair.** Verdict in
   `runs/20260827T205641Z_s3/gate_m1w-v2-k02.json`. As always, the file is everything the
   environment discloses.
2. **Your ANCHORS_F judgement call: endorsed.** Keep the exact-zero moderation floor for
   the entry. Your three reasons stand on their own; the operator adds one more: the
   moderator-abstention posture is also the single technique in this project's history
   that transferred blind. The ATE-scaled rule stays pre-registered for v2 consideration
   with the synthesiser hook unused — do not carry it into a deposit candidate unless a
   gate-grade test says to.
3. **Budget approved:** the 1–2 children you asked for (target-arm text-reading pass to
   firm up `s(a)`; a `donation_ams` / `newsletter_signup` level anchor). Same scoping.
   Nothing for `claude -p` without asking.
4. **The internal test is not this session's business.** When and whether the arm takes
   its one blind shot is an operator decision made outside these sessions; do not plan
   around it beyond keeping the entry in a state you would stand behind.

## This session

1. **Fold the verdict into the durable method.** Whatever the gate said, DESIGN.md should
   end this session stating exactly which mechanisms are promoted, which are rejected
   with diagnosis, and which are open — the standing method a fresh session would apply.
2. **Target entry v2.** Carry the promoted state of the method into
   `tools/target_model.py`, refresh the draft table end to end (synthesis, `verify()`,
   validator), and record what changed v1 → v2 and why. Still a draft; still no deposit
   actions.
3. **Fresh validation budget, spent as questions** — including none, if no question
   clears your own R-rules bar; you have shown that discipline before. If a next
   mechanism candidate emerges, declare the (candidate, baseline, k) for the gate as
   usual.
4. **The two approved children** (§ answer 3), folded into the entry where they move it.
5. **A9/A10** (the control-level anchor disagreement; the residual interaction error):
   resolve or bound them if a child or an offline analysis can; otherwise leave them
   crisply stated for the operator.
6. **Report** as always. Include your view of what, if anything, still separates this arm
   from "the entry I would stand behind" — the operator reads that section first.

Budget for this session: the launcher's defaults.
