# TASK_05 — the assertion-match question, and closing the evidence gaps you named

Session s4's REPORT.md was read in full. Operator answers first.

## Operator answers (REPORT §5)

1. **No gate was run, as you asked.** Keeping the slot was the right call, and the
   reasoning in §2 is endorsed as written: the gate is for mechanisms your own evidence
   has not already decided.
2. **Budget approved:** your two named children — first choice, a third many-variant
   train analogue for A13 (predicting a study's optimal mix from its brief); second, an
   independent read on A14 (`donation_ams` behavioural follow-through / sign). Same
   scoping rules. Nothing for `claude -p` without asking.
3. Nothing else is blocked on you.

## This session

1. **Spend the first two scored calls on the question you designed** (§6.1): isolating
   whether assertion-match contrasts carry the skill your kim-vs-beall/goldwert contrast
   suggests, in a form that bounds `ASSERTION_MATCH`'s scale by measurement instead of
   argument. Fix the reading rule in writing before scoring, as you did with
   PREREG_sub2.md. Remaining calls as questions under your own R-rules, or declined with
   reasons — both are precedented now.
2. **The two approved children** (answer 2), folded into the entry where they move it.
3. **Entry v3 if the evidence warrants it**, through the full pipeline as always; v2
   artefacts kept for the diff. Still a draft; still no deposit actions.
4. **Declare a gate candidate only if something earns it.** A session that ends with
   "the promoted state stands" is a complete session.
5. **Report** as always — and in §"what separates this arm", state plainly whether the
   method is at a stable point, and which of A13/A14/A15 (or anything new) still moves
   entry numbers materially if resolved. The operator reads that section first.

Budget for this session: the launcher's defaults.
